name: CI

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  test:
    uses: qol-tools/qol-cicd/.github/workflows/lib-ci.yml@main
    with:
      system_deps: "libxkbcommon-dev libxkbcommon-x11-dev libxcb1-dev"
